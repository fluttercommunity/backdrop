name: analysis
on: [push, pull_request]

jobs:
  package-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Flutter
        uses: subosito/flutter-action@v1

      - name: Running pub get
        run: |
          flutter pub get
          (cd demo && flutter pub get)

      - name: Analyze Dart
        run: flutter anayze . --fatal-infos

      - name: Validate Dart Formatting
        run: |
          flutter format .
          ./validate-formatting.sh
